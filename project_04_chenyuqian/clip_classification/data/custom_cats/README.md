# Custom Cat Images

Put arbitrary cat images here for evaluation-only analysis.

Supported extensions: `.jpg`, `.jpeg`, `.png`, `.bmp`, `.webp`.

Run:

```bash
python cat_alignment_analysis.py --custom-cat-dir data/custom_cats
```

These images are never used for training or prompt tuning.
