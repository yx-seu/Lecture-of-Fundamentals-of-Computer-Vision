# Result Figures

This directory is used for exported report figures such as dataset samples, training curves, prediction grids, failure cases, and confusion matrices.

Figures are generated by `src.train`, `src.evaluate`, and `src.visualize`, then copied here with:

```bash
python -m src.export_submission --output_dir outputs/segformer_b0_pet --results_dir results
```
